# Pi-estimation-using-monte-carlo-simulation
A Python program that estimates the value of π using a Monte Carlo simulation, it randomly generates a point in a square and uses the ratio between how many points lie in a quarter circle (simulation runs 10,000 times and generates 1000 random points per simulation).
